# Текущее состояние блока A

Состояние повторно сверено 10 сентября 2026 года по таблице
`Задачи_команды_ИБ_агент.xlsx`, ветке `security/c15-fix-validation` и местным
проверкам.
Столбец состояния в таблице не обновлялся и больше не отражает фактический код.

| ID | Состояние | Что уже есть | Что ещё требуется |
|---|---|---|---|
| A-01 | готово | разобраны точка запуска и путь `instruction → driver → tool → observation → validation`; схема записана в `A01_A03_REPORT.md` | — |
| A-02 | готово в коде | единые `DriverContext`, `AgentAction`, `ToolResult`, каталог действий и независимая проверка | организационно подтвердить владельцев файлов всей командой |
| A-03 | готово | переменные модели, безопасная сводка, пробный запрос и совместимый клиент; соединение проверено через Gemini | повторить на итоговом локальном адресе соревнования |
| A-04 | готово местно | `hello-file` и `bye-file` прошли через архивный `run.sh` с точными файлами и без обращения к модели | повторить в итоговом Linux-образе |
| A-05 | готово | `local_agent.py` — точка входа, ядро разделено на модули | — |
| A-06 | готово | обращения, реальные входные/выходные токены, отдельная оценка, время и причина остановки; поле `usage` проверено через Gemini | повторить на итоговой модели соревнования |
| A-07 | готово в коде | общий монотонный срок, остаток времени в контексте и ограничение отдельного запроса/команды | проверить на итоговом медленном адресе |
| A-08 | готово в коде | ошибки файловых, процессных, ИБ- и CTF-инструментов возвращаются как ограниченный `ToolResult` | — |
| A-09 | готово в коде | одинаковые действия и отсутствие продвижения ограничены и приводят к управляемому восстановлению либо остановке | дополнительно проверить с итоговой моделью |
| A-10 | готово в коде | модели передаются ограниченные инструкции, стратегии, схемы действий и последние события | измерение вынесено в A-13 |
| A-11 | готово в ветке C15 | режимы audit/fix/forensics/ctf/general, отдельные стратегии, ветвление гипотез и разрешения действий объединены | проверить после итогового слияния веток |
| A-12 | готово в коде | `finish` вызывает независимую проверку артефактов, изменений, синтаксиса и разрешённых команд | расширять правила под новые классы задач |
| A-13 | принято как готовое | воспроизводимое сравнение 24 и 6 событий прошло 4/4 с экономией 69,70%; Gemini подтвердил 69,15% экономии фактических токенов на двух завершённых сценариях | полный прогон Gemini ограничила квота 20 запросов; повторить на итоговой модели необязательно для принятого результата |
| A-14 | готово | срок запроса, ограниченные повторы временных ошибок и общий срок выполнения; настоящие 429/503 и отсутствие ответа корректно распознаны | повторить на итоговом локальном адресе соревнования |
| A-15 | готово местно | отдельный набор проверяет изменённые имена, пути, Unicode, окончания строк, неполные исправления, ограничения процессов и отсутствие сети | повторить полный запуск в итоговом Linux-образе |
| A-16 | готово местно | от C15 собран воспроизводимый архив: 53 файла, 131 292 байта, одинаковый SHA-256 при двух сборках; все доступные проверки прошли (203+79), платформенные проверки корректно пропущены (11+3) | после согласованного слияния веток повторить сборку и запуск в Linux-контейнере |

Ближайшие действия блока A: согласовать слияние командных веток, повторить
сборку из объединённой версии и проверить её в образе соревнования.
