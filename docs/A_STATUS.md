# Текущее состояние блока A

Состояние определено по таблице `Задачи_команды_ИБ_агент.xlsx`, коду ветки
`origin/main` и местным проверкам от 6 сентября 2026 года.

| ID | Состояние | Что уже есть | Что ещё требуется |
|---|---|---|---|
| A-01 | готово | разобран официальный `run.sh`, `agent.py`, `local_agent.py`; схема записана в A01_A03_REPORT | — |
| A-02 | в работе | единые `DriverContext`, `AgentAction`, `ToolResult`, реестр действий и проверка результата | подтвердить владельцев файлов всей командой |
| A-03 | в работе | проверка переменных, безопасная сводка, пробный запрос, тестовый сервер | выполнить пробный запрос к настоящему локальному адресу |
| A-04 | готово местно | `hello-file` и `bye-file` проходят, 0 запросов к модели | повторить в образе `secureintelligent/acp` |
| A-05 | готово местно | `local_agent.py` — точка входа; ядро разделено на модули | повторить A-04 в Linux-контейнере |
| A-06 | готово в коде | обращения, реальные входные/выходные токены, отдельная оценка, время и причина остановки | сверить поле `usage` настоящего адреса |
| A-07 | готово в коде | общий монотонный срок и ограничение отдельного запроса | проверить на медленном настоящем адресе |
| A-08 | частично | ошибки имеющихся инструментов превращаются в наблюдение | повторить после добавления общих файловых и процессных действий |
| A-09 | готово в коде | одинаковые действия ограничены и приводят к управляемому завершению | проверить с настоящей моделью |
| A-10 | готово в коде | модели передаются последние 6 событий, сокращённые результаты и стратегии | измерить экономию на длинном прогоне |
| A-11 | частично | выбор audit/fix/forensics/general и загрузка стратегий уже работают | нужны общие инструменты для неизвестных задач |
| A-12 | готово в коде | `finish` всегда вызывает независимую проверку артефактов, изменений, синтаксиса и команд | расширять правила под новые типы задач |
| A-13 | не начато | — | сравнить варианты контекста на одинаковом наборе |
| A-14 | готово в коде | срок запроса, два ограниченных повтора временных ошибок, общий срок | проверить на настоящем временном сбое адреса |
| A-15 | частично | все 6 официальных задач и проверки C-06…C-09 проходят на одноразовых копиях | прогнать полный `run.sh` в Linux и дополнительные варианты |
| A-16 | не начато | — | после объединения, прогонов и упаковки |

Главная зависимость следующего этапа — общие безопасные действия чтения,
поиска, правки и запуска процессов. Без них модель умеет выбирать действие, но
не сможет решать неизвестные закрытые задачи. В дорожной карте товарища это
также обозначено как следующий этап C-10.
